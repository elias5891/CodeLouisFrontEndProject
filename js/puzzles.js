function RNG(maxvalue) {
  return Math.floor(Math.random()*10);
}

function startEmojiMath() {
  first_tens = RNG(10);	
  first_ones = RNG(10);
  second_tens = RNG(10);	
  second_ones = RNG(10);
  
}

